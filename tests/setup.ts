// Import extra DOM matchers from @testing-library/jest-dom
// This adds helpful assertions like `.toBeInTheDocument()`, `.toHaveTextContent()`,
// `.toHaveAttribute()`, etc. Without this import, Jest wouldn't understand those methods.

import "@testing-library/jest-dom";
